# Failure Library

- missing state layer: prevents long-lived operation.
- missing handoff trace: breaks auditability of role transitions.
- scope violation: writing outside sandbox invalidates the run.
