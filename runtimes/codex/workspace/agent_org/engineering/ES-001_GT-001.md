# Engineering Spec: ES-001 GT-001 Bootstrap

Work item ID: `WI-001`
Owner: `engineering-manager`
Status: complete

Scope summary:
Implement the required `agent_org/` structure and runtime artifacts in the Codex sandbox.

Non-goals:
- No changes to `core/` or `comparison/`.
- No cross-runtime artifacts.

Required artifacts:
- All files listed in GT-001 expected result.
- One product brief, task graph, work order, status board, handoff log, integration log.
- State layer files with SQLite schema.

State layer requirements:
- SQLite schema based on `core/state/sqlite_schema_template.sql`.
- State registry mapping run, roles, work items, artifacts.

Task breakdown:
1. Create `agent_org/` structure with populated templates.
2. Create GT-001 work artifacts (brief, spec, task graph, work order).
3. Populate evaluation and learning artifacts.
4. Populate state layer and registry.
5. Create runtime-specific Codex agents and skills.
6. Record run summary and evaluation trace.

Acceptance criteria:
- All quality gates in `policies/quality_gates.md` are satisfied.

Handoffs:
- `engineering-manager -> implementation-agent`
