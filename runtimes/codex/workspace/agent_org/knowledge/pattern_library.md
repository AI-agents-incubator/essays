# Pattern Library

Purpose: record reusable organizational patterns.

Owner: `learning-agent`

Patterns:
- "Brief-to-Spec": product brief always precedes engineering spec.
- "State-Backed Cycle": every work item has a state entry.
- "Audit-Learn": benchmark results flow into improvement backlog.

Update rules:
- Append-only.
- New patterns require evidence in `evaluation/process_audits.md`.
