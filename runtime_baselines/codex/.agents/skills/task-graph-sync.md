# Skill: task-graph-sync

Purpose:
- Keep the task graph aligned with work orders.

When to use:
- After creating or updating `execution/work_orders/`.

Required inputs:
- `engineering/task_graph.md`
- `execution/work_orders/`

Expected outputs:
- Updated task graph with current work order steps.

Artifacts updated:
- `engineering/task_graph.md`
